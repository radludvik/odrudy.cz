# Exporty a lokální DB (velké soubory, citlivá data)
exports/
audit/db/*.tsv
audit/api/*.json
audit/samples/*.json

# Node
app/node_modules/
app/.next/
app/scripts/output-*.json

# Aevia — generovaný výstup (sestavuje se z data/ pomocí build/build.mjs)
anti-aging-platform/site/

# Env
.env
.env.local
app/.env
app/.env.local

# Editor
.vscode/
*.DS_Store

# XAMPP DB (jen lokální)
*.sql.bak
